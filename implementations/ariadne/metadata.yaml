fullName: Ariadne
version: 0.13.0
language: Python
documentation: https://ariadnegraphql.org/docs/apollo-federation
tests:
  "@key single":
    caveat: All types of `@key` definitions are supported, but multiple `@key` directives on one object is not.
  "@key multiple":
    caveat: All types of `@key` definitions are supported, but multiple `@key` directives on one object is not.
  "@key composite":
    caveat: All types of `@key` definitions are supported, but multiple `@key` directives on one object is not.
